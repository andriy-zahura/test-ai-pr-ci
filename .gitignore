node_modules/
dist-cli/
.review-context/
*.tgz

# Local env (API keys) — contributors only, not published
.env
.env.local
.env.jti-ai-review
.env.jti-ai-review.local

# Local dogfood reports if you run ai-review manually
docs/reviews/

# jti-ai-review (managed by ai-review init)
# jti-ai-review env (API keys — .example is committed, real file is not)
.env.jti-ai-review
.env.jti-ai-review.local

# Local review reports (not committed)
docs/reviews/**
!docs/reviews/.gitkeep

# jti-ai-review (managed by ai-review init)
# jti-ai-review env (API keys — .example is committed, real file is not)
.env.jti-ai-review
.env.jti-ai-review.local
.husky/pre-commit

# Local review reports (not committed)
docs/reviews/**
!docs/reviews/.gitkeep
